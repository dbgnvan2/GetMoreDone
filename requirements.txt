# Runtime dependencies — everything the application needs to run.
#
# Test-only packages live in requirements-dev.txt. Keep them there: this file
# is the list that ships. tests/test_release_licensing.py reads both files and
# checks the GPL rule against *this* one only, so a package added here is a
# package asserted to be shippable inside the binary.

# GUI Libraries (uncomment the one you want to use)
# Option 1: CustomTkinter (modern themed tkinter)
customtkinter>=5.2.0
pygame>=2.5.0  # Audio playback for timer music

# Option 2: PyQt6 (more powerful, professional)
# PyQt6>=6.6.0
# PyQt6-sip>=13.6.0

# Option 3: Kivy (cross-platform, touch-friendly)
# kivy>=2.2.0

# Database
# SQLite is built into Python, no additional package needed

# Utilities
python-dotenv>=1.0.0
platformdirs>=4.0.0  # Correct per-OS user data dirs (DB/settings) + PyInstaller-friendly
Pillow>=10.0.0  # Image handling for icons
pyobjc-framework-Cocoa>=9.0; sys_platform == "darwin"  # macOS Dock icon (replaces the Python launcher rocket at runtime)

# Google Calendar Integration
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.110.0
tzlocal>=5.0.0  # Automatic timezone detection for calendar events
