# Handoff Note

- Date: 2026-08-21
- Agent: GitHub
- Topic: v0.3.0 release — version bump, changelog versioning, tag and push

## Summary

Cut the 0.3.0 release. The `[Unreleased]` changelog section already held all the
accumulated work (daVIPA rebrand, item-editor project links, weekly-tactic
scheduling, rename-safe links, Who/Context fixes, release-workflow hardening), so
the release was mechanical: version it, bump the package version, tag, push.

`src/getmoredone/__init__.py.__version__` had never been bumped — it still read
`0.1.0` through the 0.2.0 release — so it goes straight to `0.3.0`. Nothing in
the app reads it yet (no About dialog); it exists for correctness and future use.

The changelog `[Unreleased]` block was left intact rather than reorganised: its
content is accurate and complete, and the release-notes extractor
(`tools/extract_release_notes.py`) reads the whole section regardless of how many
`### Added`/`### Changed`/`### Fixed` subheadings it carries.

## Files changed

- `CHANGELOG.md` — `[Unreleased]` → `[0.3.0] - 2026-08-21`, fresh `[Unreleased]`
  above it, bottom link references updated (`[0.3.0]` compare link added).
- `src/getmoredone/__init__.py` — `__version__` `0.1.0` → `0.3.0`.
- `docs/changes/2026-08-21-v0.3.0-release.md` — this note.

## Verification

- Command: `./venv/bin/python -m pytest -q tests/test_release_notes.py tests/test_release_docs.py`
- Result: see run output (expected PASS — the changelog heading and link edits are
  the exact contracts these tests assert).

## Risks / Known gaps

- The `### Added`/`### Changed`/`### Fixed` subheadings inside `[0.3.0]` are not
  consolidated into single blocks per Keep a Changelog convention. Cosmetic; not
  fixed in this release.
- The changelog preamble still says "All notable changes to GetMoreDone are
  recorded here" while the app is now daVIPA. Cosmetic; left for a follow-up.
- Builds remain unsigned (macOS Gatekeeper step still required; see INSTALL.md).

## Next agent actions

- Watch the `Build binaries` workflow run triggered by the `v0.3.0` tag; confirm
  `publish` succeeds and the four assets appear on the release page.
- Optionally consolidate the changelog subheadings and update the preamble prose.
