# Handoff Note

- Date: 2026-08-18
- Agent: Code
- Topic: weekly-tactic-scheduling — complete (WT-M1 through WT-M7)

## Summary

`docs/spec_2026-08-18_weekly_tactic_scheduling.md` is fully implemented against
`docs/implementation_plan_2026-08-18_weekly_tactic_scheduling.md`.

**All 83 leaf acceptance criteria have a collected test.** See
`docs/spec_coverage_2026-08-18_weekly_tactic_scheduling.md` for the ID-by-ID map,
which is generated by cross-checking the spec's own `→ tests/...` references
against `pytest --collect-only` rather than written by hand.

## Verification

- `./venv/bin/python -m pytest -q` → **exit 0, 834 passed, 2 skipped**. Baseline
  before this work: 679 passed, 2 skipped.
- Every new test file passes in isolation.
- **In the running app** (real Tk root, real screens, real database): the Org
  tab, the Today screen, both VSP screens rendering a blank rollover stub, a
  year rollover end to end, a mid-session first-day-of-week change, and a bulk
  edit across a year boundary reporting what it built.
- **Against the live database**, two consecutive launches: the repair moves 0
  rows and `reschedule_history` stays at 2,129.
- **learning-qa run four times** — once per commit, including three passes over
  fix commits. 9 + 10 + 13 + 9 findings. Two of those rounds showed that a
  previous round's headline fix had not actually landed.

## What the sweeps caught that the tests did not

Recorded because the pattern is the lesson, not the individual bugs:

- **A hand-written list of tables** referencing `action_items` was wrong twice
  over — one missing table crashed the app at start-up, another was silently
  destroying rows. The list is derived from `PRAGMA foreign_key_list` now.
- **`except Exception` in the transaction manager** did not catch `KeyboardInterrupt`,
  which left the commit gate closed for the life of the process. Every later
  save looked fine and was discarded at close. `./start.sh` runs from a terminal.
- **Two sources of truth for the week boundary** — a cached calendar and a live
  setting — meant changing "First day of week" mid-session lost the next save.
- **Guards that could not fail**: the week-math scan was green on the defect it
  was written for; the wiring guard was satisfied by an import line; a
  `assert ... or True`.
- **Found by running the app, not by the suite**: an item *created* already
  attached to a tactic was never stamped, and picking a tactic by hand was
  silently overridden by the item's own start date.

## Live database

The migration and repair have already run against it — you launched the app
mid-build. Verified on a read-only copy:

- the WT-F5 duplicate merged onto the row holding all five children, its title
  corrected from `W8` to `PW|LS|Blog - W9`, the loser's history row repointed;
- **29 items had their dates moved back inside their Weekly Tactic's week**,
  each with a `reschedule_history` row carrying `reason='inv_repair'`;
- all 94 nesting links and all history rows intact; the WT-INV5 index in place.

**This is the one thing still needing your eyes** — see below.

## Follow-ups

1. **Review the 29 repaired items.** The full before/after list is in
   `~/Library/Application Support/GetMoreDone/weekly_tactic_debug.log`. Some
   moves are large (one is 77 days) because the item's date was far from the
   week its tactic covers. Every move is reversible from its `inv_repair`
   history row. If any of those dates was deliberate rather than drift, it needs
   putting back by hand.
2. **`RescheduleDialog` has no caller in `src/`** — recorded in `BACKLOG.md`.
3. **Whether a blank rollover stub *reads* as a real plan** is a judgement only
   you can make. It renders without error and its text is empty, not invented.

## Files

New source: `week_calendar.py`, `weekly_tactic.py`, `weekly_tactic_migrations.py`,
`weekly_tactic_maintenance.py`, `weekly_tactic_titles.py`, `weekly_tactic_logging.py`,
`screens/week_collision_notice.py`.

New tests: `test_weekly_tactic_link_migration.py`, `test_weekly_tactic_schema.py`,
`test_week_numbering.py`, `test_weekly_tactic_dedupe.py`, `test_weekly_tactic_linking.py`,
`test_weekly_tactic_cascade.py`, `test_weekly_tactic_completion.py`,
`test_weekly_tactic_surfaces.py`, `test_item_editor_weekly_tactic_ui.py`,
`test_project_board_dates_ui.py`, `test_settings_week_rule_ui.py`, plus
`tests/weekly_tactic_fixtures.py`.

Changed: `database.py`, `db_manager.py`, `db_manager_project_boards.py`, `models.py`,
`app_settings.py`, `calendar_importer.py`, `vps_manager.py`, `vps_manager_planning.py`,
`conftest.py`, and eleven screens.
