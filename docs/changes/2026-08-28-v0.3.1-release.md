# Handoff Note

- Date: 2026-08-28
- Agent: Code
- Topic: v0.3.1 release — repair a published release with no assets

## Summary

`v0.3.1` had already been tagged, pushed and published as "Timer Functioning
Enhancements", but the release page carried **zero assets**. Both build jobs
succeeded; the `publish` job failed on one step:

```
error: CHANGELOG.md has no section for 'v0.3.1'. Available: ['Unreleased', '0.3.0', '0.2.0']
```

The tag was pushed before the changelog was versioned, and
`tools/extract_release_notes.py` refuses to invent a section rather than
publishing an empty release body — which is why the failure was loud and the
release stayed empty instead of shipping unlabelled binaries.

Fixed by doing what the 0.3.0 handoff note records: version the changelog, bump
the package version, then move the tag onto that commit so the assets and the tag
name point at the same source.

## Files changed

- `CHANGELOG.md` — `[Unreleased]` → `[0.3.1] - 2026-08-28`, fresh empty
  `[Unreleased]` above it, `[0.3.1]` compare link added and `[Unreleased]`
  re-pointed at `v0.3.1...HEAD`.
- `src/getmoredone/__init__.py` — `__version__` `0.3.0` → `0.3.1`.
- `docs/changes/2026-08-28-v0.3.1-release.md` — this note.

`README.md` needed no change: its download link is `.../releases` with no version
in it, so it already points at whatever is latest.

## Verification

- Command: `taskpolicy -b ./venv/bin/python -m pytest tests/test_release_notes.py tests/test_release_docs.py tests/test_ci_contract.py -q`
- Result: PASS — 123 passed, 1 skipped (the skip is CI-only, needs a display).
- The failing CI step reproduced locally before and after:
  `python tools/extract_release_notes.py v0.3.1` now writes 4166 chars instead of
  exiting 1.

## Risks / Known gaps

- **The `v0.3.1` tag was force-moved.** It previously pointed at `70e045d`; it now
  points at the changelog commit. Defensible here because the release had no
  assets, so nothing could have been downloaded against the old target — but it is
  a rewrite of a published ref and should not become habit. The alternative
  considered was `workflow_dispatch` with `release_tag`, which leaves the tag alone
  but builds from `main`, so the assets and the tag would name different commits.
- Builds remain unsigned; the macOS Gatekeeper step in `INSTALL.md` still applies.
- The `[0.3.1]` section keeps the `### Added`/`### Changed`/`### Fixed`
  subheadings as they accumulated, rather than consolidated per Keep a Changelog.
  Same cosmetic gap the 0.3.0 note recorded.

## Next agent actions

- Nothing outstanding once the workflow's `publish` job is green and four assets
  appear on the release page.
